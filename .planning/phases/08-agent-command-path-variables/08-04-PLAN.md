---
phase: 08-agent-command-path-variables
plan: 04
type: execute
wave: 3
depends_on: [08-01, 08-02, 08-03]
files_modified:
  - get-shit-done/bin/lib/template.cjs
  - get-shit-done/bin/lib/commands.cjs
  - get-shit-done/bin/lib/milestone.cjs
  - get-shit-done/bin/lib/verify.cjs
  - get-shit-done/bin/lib/config.cjs
  - get-shit-done/bin/gsd-tools.cjs
autonomous: true
requirements: [INT-02, INT-03, INT-04]

must_haves:
  truths:
    - "bin/ files with convertible refs (template.cjs, commands.cjs, milestone.cjs, verify.cjs, config.cjs, gsd-tools.cjs) use resolved paths"
    - "Structural refs in core.cjs and migration.cjs are intentionally preserved"
    - "Final comprehensive grep across ALL GSD directories shows only structural refs in core.cjs and migration.cjs"
  artifacts:
    - path: "get-shit-done/bin/lib/template.cjs"
      provides: "Plan template generation with namespace-resolved @-paths"
    - path: "get-shit-done/bin/lib/commands.cjs"
      provides: "Default staging path uses resolved path"
  key_links:
    - from: "get-shit-done/bin/lib/template.cjs"
      to: "plan PLAN.md files"
      via: "Generates @-path file refs in plan content using resolved planningRoot"
      pattern: "planRoot|planningRoot"
    - from: "get-shit-done/bin/lib/verify.cjs"
      to: "planning state"
      via: "Reads state content from resolved path"
      pattern: "planRoot|planningRoot"
---

<objective>
Convert ~8 convertible .planning/ refs across 6 bin/ files to namespace-resolved paths, then run the final comprehensive verification sweep across ALL GSD directories.

Purpose: Close the bin/ gap identified in CONTEXT.md full sweep scope. bin/ files were converted in Phase 2 but some refs remain. After this plan, the only .planning/ literals in the entire codebase are structural refs in core.cjs and migration.cjs (which define the .planning/ abstraction itself).
Output: 6 updated bin/ files + verified zero-hit sweep across all GSD directories
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-command-path-variables/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert bin/ files with non-structural .planning/ refs</name>
  <files>
    get-shit-done/bin/lib/template.cjs
    get-shit-done/bin/lib/commands.cjs
    get-shit-done/bin/lib/milestone.cjs
    get-shit-done/bin/lib/verify.cjs
    get-shit-done/bin/lib/config.cjs
    get-shit-done/bin/gsd-tools.cjs
  </files>
  <action>
Convert non-structural .planning/ refs in each bin/ file. These are JavaScript files -- use the resolved `planRoot` variable (from `planningRoot()` function already available in most modules).

Per-file analysis from RESEARCH.md:

**template.cjs (3 refs -- all convert):**
- `@.planning/PROJECT.md`, `@.planning/ROADMAP.md`, `@.planning/STATE.md` in plan template generation
- These generate plan file content -- must use namespace-aware paths
- Replace with template literals using the resolved planRoot: `` `@${planRoot}/PROJECT.md` ``

**commands.cjs (1 ref -- convert):**
- Default `files` arg `['.planning/']` for staging
- Replace with resolved path from planningRoot()

**milestone.cjs (1 ref -- convert):**
- Archive header text `see .planning/REQUIREMENTS.md`
- Replace with resolved path

**verify.cjs (3 refs -- 2 convert, 1 structural):**
- `.planning/ directory not found` error message -- STRUCTURAL, keep as-is (describes the concept)
- `.planning/PROJECT.md` state content ref -- convert to resolved path
- Other state content ref -- convert to resolved path

**config.cjs (2 refs -- 1 convert, 1 keep):**
- `result.path: '.planning/config.json'` display-only output -- convert to show resolved path
- `.planning/.gitignore` comment -- STRUCTURAL description, keep

**gsd-tools.cjs (2 refs -- convert per user decision):**
- Help text/comments referencing .planning/ -- per user decision "variablize ALL references," convert these to use resolved path or generic description

For each file: read the full file first, understand the context of each ref, then convert only the non-structural ones. Each module already imports or has access to `planningRoot()` from core.cjs -- use the existing resolved path variable.
  </action>
  <verify>
    <automated>echo "=== bin/ ref check ===" && for f in get-shit-done/bin/lib/template.cjs get-shit-done/bin/lib/commands.cjs get-shit-done/bin/lib/milestone.cjs get-shit-done/bin/lib/verify.cjs get-shit-done/bin/lib/config.cjs get-shit-done/bin/gsd-tools.cjs; do count=$(grep -c '\.planning/' "$f" 2>/dev/null); echo "$(basename $f): $count refs"; done && echo "=== Structural exceptions (expected) ===" && for f in get-shit-done/bin/lib/core.cjs get-shit-done/bin/lib/migration.cjs; do count=$(grep -c '\.planning/' "$f" 2>/dev/null); echo "$(basename $f): $count refs (structural)"; done && echo "=== npm test ===" && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>template.cjs has 0 .planning/ refs. commands.cjs has 0. milestone.cjs has 0. verify.cjs has at most 1 structural ref. config.cjs has at most 1 structural ref. gsd-tools.cjs has 0. npm test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Final comprehensive verification sweep across all GSD directories</name>
  <files></files>
  <action>
Run a final grep across ALL GSD directories to verify zero .planning/ hardcoded refs remain (except documented structural exceptions):

1. Check agents/: expect 0 hits (converted in Plan 01)
2. Check commands/gsd/: expect 0 hits (converted in Plan 02)
3. Check get-shit-done/templates/: expect 0 hits (main templates converted Phase 3, subdirectories in Plan 03)
4. Check get-shit-done/references/: expect 0 hits (converted in Plan 03)
5. Check hooks/: expect ONLY structural refs in gsd-context-monitor.js (.planning/.active, .planning/ directory concept)
6. Check get-shit-done/bin/: expect ONLY structural refs in core.cjs (planningRoot, writeActiveFile, listNamespaces) and migration.cjs (flat layout detection). Verify.cjs and config.cjs may have 1 structural ref each (error messages describing .planning/ concept).
7. Check get-shit-done/workflows/: expect 0 hits (converted in Phase 3)

Run `npm test` to confirm no regressions from all changes across Plans 01-04.

Document the final structural exception list:
- get-shit-done/bin/lib/core.cjs: ~5 refs (defines planningRoot, writeActiveFile, listNamespaces)
- get-shit-done/bin/lib/migration.cjs: ~8 refs (flat layout detection, migration logic)
- hooks/gsd-context-monitor.js: ~2-3 refs (.planning/.active structural detection)
- get-shit-done/bin/lib/verify.cjs: 0-1 ref (error message about .planning/ directory concept)
- get-shit-done/bin/lib/config.cjs: 0-1 ref (.gitignore comment)

If any non-structural refs found in any directory, fix them in this task.
  </action>
  <verify>
    <automated>echo "=== Full sweep ===" && for dir in agents commands/gsd get-shit-done/templates get-shit-done/references get-shit-done/workflows; do count=$(grep -rl '\.planning/' "$dir" 2>/dev/null | wc -l | tr -d ' '); echo "$dir: $count files with refs"; if [ "$count" -ne "0" ]; then echo "UNEXPECTED REFS:"; grep -rn '\.planning/' "$dir" 2>/dev/null; fi; done && echo "=== Structural exceptions (expected) ===" && grep -rl '\.planning/' get-shit-done/bin/ hooks/ 2>/dev/null && echo "=== Test suite ===" && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>Zero .planning/ refs in agents/, commands/, templates/, references/, workflows/. Only structural refs remain in bin/core.cjs, bin/migration.cjs, hooks/gsd-context-monitor.js (plus optional structural refs in verify.cjs, config.cjs). npm test passes. Structural exception list documented in summary.</done>
</task>

</tasks>

<verification>
- `grep -rl '\.planning/' agents/ commands/gsd/ get-shit-done/templates/ get-shit-done/references/ get-shit-done/workflows/` returns zero results
- `grep -rl '\.planning/' get-shit-done/bin/ hooks/` returns ONLY structural exceptions (core.cjs, migration.cjs, gsd-context-monitor.js, optionally verify.cjs/config.cjs)
- `npm test` passes with zero failures
</verification>

<success_criteria>
- All convertible bin/ refs replaced with resolved paths
- Structural refs in core.cjs and migration.cjs preserved
- Final sweep confirms zero non-structural .planning/ refs across entire GSD codebase
- npm test green
- Structural exception list documented
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-command-path-variables/08-04-SUMMARY.md`
</output>
