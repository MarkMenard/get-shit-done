---
phase: 08-agent-command-path-variables
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - commands/gsd/complete-milestone.md
  - commands/gsd/map-codebase.md
  - commands/gsd/new-project.md
  - commands/gsd/new-milestone.md
  - commands/gsd/research-phase.md
  - commands/gsd/debug.md
  - commands/gsd/cleanup.md
  - commands/gsd/audit-milestone.md
  - commands/gsd/add-tests.md
  - commands/gsd/switch-project.md
  - commands/gsd/quick.md
  - commands/gsd/plan-milestone-gaps.md
  - commands/gsd/health.md
autonomous: true
requirements: [INT-04]

must_haves:
  truths:
    - "add-tests.md uses variable placeholders instead of @.planning/ file references"
    - "All 13 command .md files have zero hardcoded .planning/ path literals"
    - "Command files that had @.planning/ file-loading refs either remove them (workflow handles loading) or convert to variable notation"
  artifacts:
    - path: "commands/gsd/add-tests.md"
      provides: "Namespace-aware add-tests command"
    - path: "commands/gsd/complete-milestone.md"
      provides: "Namespace-aware complete-milestone command"
    - path: "commands/gsd/map-codebase.md"
      provides: "Namespace-aware map-codebase command"
  key_links:
    - from: "commands/gsd/*.md"
      to: "workflows/*.md"
      via: "Commands delegate to workflows via <execution_context>; workflows resolve paths via init"
      pattern: "execution_context"
---

<objective>
Replace all hardcoded .planning/ references across 13 command .md files with variable placeholders or removal (where @-prefix file loading is involved).

Purpose: Close INT-04 (add-tests.md uses hardcoded refs) and convert all remaining command files. Commands delegate to workflows which resolve namespaces via init, so @.planning/ file-loading refs should be removed and notes added that workflows handle loading.
Output: 13 converted command files, zero .planning/ literals in commands/gsd/
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-command-path-variables/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert high-ref command files (4 files, 29 refs)</name>
  <files>
    commands/gsd/complete-milestone.md
    commands/gsd/map-codebase.md
    commands/gsd/new-project.md
    commands/gsd/new-milestone.md
  </files>
  <action>
For each of the 4 highest-ref command files, replace ALL .planning/ references:

**Two conversion patterns for commands:**

1. **@.planning/ file-loading refs** (e.g., `@.planning/STATE.md`): These are Claude Code literal path refs resolved at command invocation time BEFORE namespace resolution happens. Remove them entirely and add a comment that the workflow handles file loading after namespace resolution. Example:
```markdown
<!-- Before -->
@.planning/STATE.md
@.planning/ROADMAP.md

<!-- After -->
<!-- State and roadmap loaded by workflow after namespace resolution -->
```

2. **Instructional/documentation .planning/ refs** (e.g., "Create `.planning/milestones/..."): Replace with ${planning_root} variables:
```markdown
<!-- Before -->
Create `.planning/milestones/v1.0/`

<!-- After -->
Create `${planning_root}/milestones/v1.0/`
```

Expected ref counts:
- complete-milestone.md: 11 refs
- map-codebase.md: 7 refs
- new-project.md: 6 refs
- new-milestone.md: 5 refs

Process each file completely. Preserve all other content unchanged.
  </action>
  <verify>
    <automated>count=$(grep -c '\.planning/' commands/gsd/complete-milestone.md commands/gsd/map-codebase.md commands/gsd/new-project.md commands/gsd/new-milestone.md 2>/dev/null | tail -1 | cut -d: -f2); echo "Refs remaining: $count"; if [ "$count" -ne "0" ]; then grep -n '\.planning/' commands/gsd/complete-milestone.md commands/gsd/map-codebase.md commands/gsd/new-project.md commands/gsd/new-milestone.md; exit 1; fi; echo "PASS"</automated>
  </verify>
  <done>complete-milestone.md, map-codebase.md, new-project.md, and new-milestone.md have zero .planning/ literals.</done>
</task>

<task type="auto">
  <name>Task 2: Convert remaining 9 command files (16 refs)</name>
  <files>
    commands/gsd/research-phase.md
    commands/gsd/debug.md
    commands/gsd/cleanup.md
    commands/gsd/audit-milestone.md
    commands/gsd/add-tests.md
    commands/gsd/switch-project.md
    commands/gsd/quick.md
    commands/gsd/plan-milestone-gaps.md
    commands/gsd/health.md
  </files>
  <action>
Apply the same two conversion patterns as Task 1 to the remaining 9 command files:

1. **@.planning/ file-loading refs**: Remove, note workflow handles loading
2. **Instructional .planning/ refs**: Replace with ${planning_root} variables

Expected ref counts:
- research-phase.md: 3 refs
- debug.md: 3 refs
- cleanup.md: 2 refs
- audit-milestone.md: 2 refs
- add-tests.md: 2 refs (INT-04 specifically: `@.planning/STATE.md` and `@.planning/ROADMAP.md`)
- switch-project.md: 1 ref
- quick.md: 1 ref
- plan-milestone-gaps.md: 1 ref
- health.md: 1 ref

For add-tests.md specifically (INT-04): The `<context>` block contains `@.planning/STATE.md` and `@.planning/ROADMAP.md`. Remove these @-refs since the workflow will load state and roadmap after namespace resolution.

Process each file completely.
  </action>
  <verify>
    <automated>count=$(grep -rl '\.planning/' commands/gsd/ 2>/dev/null | wc -l | tr -d ' '); echo "Command files with .planning/ refs: $count"; if [ "$count" -ne "0" ]; then echo "FAIL"; grep -rn '\.planning/' commands/gsd/; exit 1; fi; echo "PASS"</automated>
  </verify>
  <done>All 13 command files have zero .planning/ literals. add-tests.md @-refs removed per INT-04.</done>
</task>

</tasks>

<verification>
- `grep -rl '\.planning/' commands/gsd/` returns zero results
- `npm test` passes (commands are static markdown, low regression risk)
</verification>

<success_criteria>
- All 13 command files have zero .planning/ hardcoded refs
- add-tests.md specifically has @.planning/ refs removed (INT-04)
- @-prefix file-loading refs removed with notes about workflow handling
- Instructional refs converted to ${planning_root} variables
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-command-path-variables/08-02-SUMMARY.md`
</output>
