---
phase: 08-agent-command-path-variables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-context-monitor.js
  - agents/gsd-planner.md
  - agents/gsd-debugger.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
  - agents/gsd-executor.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-verifier.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-integration-checker.md
autonomous: true
requirements: [INT-02, INT-03]

must_haves:
  truths:
    - "Context monitor detects GSD via .planning/.active slug resolution, not hardcoded flat path"
    - "All 10 agent .md files use ${planning_root} variables instead of hardcoded .planning/ paths"
    - "Agent variable placeholders follow Phase 3 naming convention (planning_root, phase_dir, state_path, etc.)"
  artifacts:
    - path: "hooks/gsd-context-monitor.js"
      provides: "Namespace-aware GSD detection"
      contains: ".planning/.active"
    - path: "agents/gsd-planner.md"
      provides: "Planner agent with variable paths"
    - path: "agents/gsd-debugger.md"
      provides: "Debugger agent with variable paths"
  key_links:
    - from: "hooks/gsd-context-monitor.js"
      to: ".planning/.active"
      via: "fs.readFileSync to get slug, then check .planning/<slug>/STATE.md"
      pattern: "readFileSync.*\\.active"
    - from: "agents/*.md"
      to: "spawning workflows"
      via: "Workflows pass planning_root in Task prompt, agents use ${planning_root} notation"
      pattern: "\\$\\{planning_root\\}"
---

<objective>
Convert the context monitor hook to namespace-aware GSD detection, and replace all hardcoded .planning/ references across 10 agent .md files with ${planning_root} variable placeholders.

Purpose: Close INT-03 (hook uses hardcoded flat path) and INT-02 (agent files use hardcoded paths). The hook is JavaScript requiring functional changes; agent files are LLM prompts requiring mechanical variable substitution.
Output: 1 converted JS hook + 10 converted agent files, zero .planning/ literals in agents/ or hooks/
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-command-path-variables/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert context monitor to namespace-aware detection</name>
  <files>hooks/gsd-context-monitor.js</files>
  <action>
Replace the hardcoded GSD detection on line ~105:
```javascript
const isGsdActive = fs.existsSync(path.join(cwd, '.planning', 'STATE.md'));
```

With namespace-aware detection per user decision:
```javascript
const activePath = path.join(cwd, '.planning', '.active');
let isGsdActive = false;
if (fs.existsSync(activePath)) {
  const slug = fs.readFileSync(activePath, 'utf8').trim();
  if (slug) {
    isGsdActive = fs.existsSync(path.join(cwd, '.planning', slug, 'STATE.md'));
  }
}
```

Key rules:
- No fallback if .active doesn't exist -- treat as "GSD not active" (per user decision)
- No flat layout fallback -- consistent with Phase 4 clean break
- Warning messages stay generic -- do NOT put resolved path in user-facing output
- The `.planning/` and `.planning/.active` literals here are STRUCTURAL (they define the directory concept), not hardcoded path usage -- this is acceptable
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('hooks/gsd-context-monitor.js', 'utf8'); const hasActive = content.includes('.active'); const noFlatDetect = !content.includes(\"path.join(cwd, '.planning', 'STATE.md')\"); console.log('Has .active check:', hasActive); console.log('No flat STATE.md check:', noFlatDetect); if (!hasActive || !noFlatDetect) process.exit(1); console.log('PASS');"</automated>
  </verify>
  <done>Context monitor reads .planning/.active for slug, checks .planning/&lt;slug&gt;/STATE.md. No flat path fallback. Generic warning messages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace .planning/ refs in all 10 agent files</name>
  <files>
    agents/gsd-planner.md
    agents/gsd-debugger.md
    agents/gsd-research-synthesizer.md
    agents/gsd-project-researcher.md
    agents/gsd-roadmapper.md
    agents/gsd-executor.md
    agents/gsd-codebase-mapper.md
    agents/gsd-verifier.md
    agents/gsd-phase-researcher.md
    agents/gsd-integration-checker.md
  </files>
  <action>
For each of the 10 agent files, replace ALL .planning/ references with the appropriate variable from Phase 3 convention:

Variable mapping:
- `.planning/PROJECT.md` or `@.planning/PROJECT.md` -> `${planning_root}/PROJECT.md` or `@${planning_root}/PROJECT.md`
- `.planning/ROADMAP.md` -> `${planning_root}/ROADMAP.md`
- `.planning/STATE.md` -> `${planning_root}/STATE.md` (or `${state_path}` when standalone)
- `.planning/REQUIREMENTS.md` -> `${planning_root}/REQUIREMENTS.md` (or `${requirements_path}`)
- `.planning/config.json` -> `${planning_root}/config.json`
- `.planning/phases/` -> `${planning_root}/phases/`
- `.planning/codebase/` -> `${codebase_dir}`
- `.planning/milestones/` -> `${planning_root}/milestones/`
- `cat .planning/X` -> `cat "${planning_root}/X"` (bash context)

Process each file completely -- no partial conversions. Expected ref counts per file:
- gsd-planner.md: 18 refs
- gsd-debugger.md: 14 refs
- gsd-research-synthesizer.md: 11 refs
- gsd-project-researcher.md: 10 refs
- gsd-roadmapper.md: 8 refs
- gsd-executor.md: 5 refs
- gsd-codebase-mapper.md: 5 refs
- gsd-verifier.md: 4 refs
- gsd-phase-researcher.md: 3 refs
- gsd-integration-checker.md: 1 ref

Agent files are LLM prompts loaded by Task tool as static system prompts. The ${planning_root} notation is interpreted by the LLM as "use the value provided in my prompt." No shell interpolation occurs.

Do NOT convert any `.planning/.active` or `.planning/<slug>/` structural references if present -- those describe the namespace mechanism itself.
  </action>
  <verify>
    <automated>count=$(grep -rl '\.planning/' agents/ 2>/dev/null | wc -l | tr -d ' '); echo "Files with .planning/ refs: $count"; if [ "$count" -ne "0" ]; then echo "FAIL - expected 0"; grep -rn '\.planning/' agents/; exit 1; fi; echo "PASS"</automated>
  </verify>
  <done>All 10 agent files have zero .planning/ literals. All refs replaced with ${planning_root}, ${phase_dir}, ${codebase_dir}, or other Phase 3 convention variables.</done>
</task>

</tasks>

<verification>
- `grep -rl '\.planning/' agents/ hooks/` returns zero results (excluding structural .planning/.active refs in hook which are acceptable)
- `npm test` passes (no regressions from hook change)
</verification>

<success_criteria>
- Context monitor uses .active-based namespace resolution
- All 10 agent files use variable placeholders
- Zero .planning/ hardcoded refs in agents/ directory
- Hook .planning/.active structural refs are the only .planning/ refs remaining in hooks/
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-command-path-variables/08-01-SUMMARY.md`
</output>
