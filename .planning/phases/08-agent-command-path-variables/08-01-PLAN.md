---
phase: 08-agent-command-path-variables
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - hooks/gsd-context-monitor.js
  - agents/gsd-planner.md
  - agents/gsd-debugger.md
  - agents/gsd-research-synthesizer.md
  - agents/gsd-project-researcher.md
  - agents/gsd-roadmapper.md
  - agents/gsd-executor.md
  - agents/gsd-codebase-mapper.md
  - agents/gsd-verifier.md
  - agents/gsd-phase-researcher.md
  - agents/gsd-integration-checker.md
autonomous: true
requirements: [INT-02, INT-03]

must_haves:
  truths:
    - "Context monitor detects GSD via .planning/.active slug resolution, not hardcoded flat path"
    - "All 10 agent .md files with refs use ${planning_root} variables instead of hardcoded .planning/ paths"
    - "The 2 zero-ref agents (gsd-nyquist-auditor.md, gsd-plan-checker.md) confirmed to have no .planning/ refs"
    - "Agent variable placeholders follow Phase 3 naming convention (planning_root, phase_dir, state_path, etc.)"
    - "Every workflow that spawns an agent passes planning_root (or the specific variable) in the Task prompt"
  artifacts:
    - path: "hooks/gsd-context-monitor.js"
      provides: "Namespace-aware GSD detection"
      contains: ".planning/.active"
    - path: "agents/gsd-planner.md"
      provides: "Planner agent with variable paths"
    - path: "agents/gsd-debugger.md"
      provides: "Debugger agent with variable paths"
  key_links:
    - from: "hooks/gsd-context-monitor.js"
      to: ".planning/.active"
      via: "fs.readFileSync to get slug, then check .planning/<slug>/STATE.md"
      pattern: "readFileSync.*\\.active"
    - from: "agents/*.md"
      to: "spawning workflows"
      via: "Workflows pass planning_root in Task prompt, agents use ${planning_root} notation"
      pattern: "\\$\\{planning_root\\}"
---

<objective>
Convert the context monitor hook to namespace-aware GSD detection, and replace all hardcoded .planning/ references across 10 agent .md files with ${planning_root} variable placeholders. Confirm the 2 remaining agents (gsd-nyquist-auditor.md, gsd-plan-checker.md) have zero refs. Verify spawning workflow wiring passes planning_root.

Purpose: Close INT-03 (hook uses hardcoded flat path) and INT-02 (agent files use hardcoded paths). The hook is JavaScript requiring functional changes; agent files are LLM prompts requiring mechanical variable substitution.
Output: 1 converted JS hook + 10 converted agent files + 2 confirmed zero-ref agents, zero .planning/ literals in agents/ or hooks/
</objective>

<execution_context>
@/Users/mark/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mark/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-agent-command-path-variables/08-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert context monitor to namespace-aware detection</name>
  <files>hooks/gsd-context-monitor.js</files>
  <action>
Replace the hardcoded GSD detection on line ~105:
```javascript
const isGsdActive = fs.existsSync(path.join(cwd, '.planning', 'STATE.md'));
```

With namespace-aware detection per user decision:
```javascript
const activePath = path.join(cwd, '.planning', '.active');
let isGsdActive = false;
if (fs.existsSync(activePath)) {
  const slug = fs.readFileSync(activePath, 'utf8').trim();
  if (slug) {
    isGsdActive = fs.existsSync(path.join(cwd, '.planning', slug, 'STATE.md'));
  }
}
```

Key rules:
- No fallback if .active doesn't exist -- treat as "GSD not active" (per user decision)
- No flat layout fallback -- consistent with Phase 4 clean break
- Warning messages stay generic -- do NOT put resolved path in user-facing output
- The `.planning/` and `.planning/.active` literals here are STRUCTURAL (they define the directory concept), not hardcoded path usage -- this is acceptable
  </action>
  <verify>
    <automated>node -e "const fs = require('fs'); const content = fs.readFileSync('hooks/gsd-context-monitor.js', 'utf8'); const hasActive = content.includes('.active'); const noFlatDetect = !content.includes(\"path.join(cwd, '.planning', 'STATE.md')\"); console.log('Has .active check:', hasActive); console.log('No flat STATE.md check:', noFlatDetect); if (!hasActive || !noFlatDetect) process.exit(1); console.log('PASS');"</automated>
  </verify>
  <done>Context monitor reads .planning/.active for slug, checks .planning/&lt;slug&gt;/STATE.md. No flat path fallback. Generic warning messages.</done>
</task>

<task type="auto">
  <name>Task 2: Replace .planning/ refs in all 10 agent files and verify workflow wiring</name>
  <files>
    agents/gsd-planner.md
    agents/gsd-debugger.md
    agents/gsd-research-synthesizer.md
    agents/gsd-project-researcher.md
    agents/gsd-roadmapper.md
    agents/gsd-executor.md
    agents/gsd-codebase-mapper.md
    agents/gsd-verifier.md
    agents/gsd-phase-researcher.md
    agents/gsd-integration-checker.md
  </files>
  <action>
**Part A: Convert agent files**

For each of the 10 agent files with .planning/ refs, replace ALL references with the appropriate variable from Phase 3 convention:

Variable mapping:
- `.planning/PROJECT.md` or `@.planning/PROJECT.md` -> `${planning_root}/PROJECT.md` or `@${planning_root}/PROJECT.md`
- `.planning/ROADMAP.md` -> `${planning_root}/ROADMAP.md`
- `.planning/STATE.md` -> `${planning_root}/STATE.md` (or `${state_path}` when standalone)
- `.planning/REQUIREMENTS.md` -> `${planning_root}/REQUIREMENTS.md` (or `${requirements_path}`)
- `.planning/config.json` -> `${planning_root}/config.json`
- `.planning/phases/` -> `${planning_root}/phases/`
- `.planning/codebase/` -> `${codebase_dir}`
- `.planning/milestones/` -> `${planning_root}/milestones/`
- `cat .planning/X` -> `cat "${planning_root}/X"` (bash context)

Process each file completely -- no partial conversions. Expected ref counts per file:
- gsd-planner.md: 18 refs
- gsd-debugger.md: 14 refs
- gsd-research-synthesizer.md: 11 refs
- gsd-project-researcher.md: 10 refs
- gsd-roadmapper.md: 8 refs
- gsd-executor.md: 5 refs
- gsd-codebase-mapper.md: 5 refs
- gsd-verifier.md: 4 refs
- gsd-phase-researcher.md: 3 refs
- gsd-integration-checker.md: 1 ref

Agent files are LLM prompts loaded by Task tool as static system prompts. The ${planning_root} notation is interpreted by the LLM as "use the value provided in my prompt." No shell interpolation occurs.

Do NOT convert any `.planning/.active` or `.planning/<slug>/` structural references if present -- those describe the namespace mechanism itself.

**Part B: Confirm zero-ref agents**

The roadmap says 12 agent files. Two agents have zero .planning/ references and need no conversion:
- `agents/gsd-nyquist-auditor.md` -- 0 refs (confirmed via grep)
- `agents/gsd-plan-checker.md` -- 0 refs (confirmed via grep)

Verify these two files genuinely have zero .planning/ refs by grepping them. Document the confirmation in the summary.

**Part C: Verify spawning workflow wiring**

After converting agent files, verify that each agent's spawning workflow(s) extract `planning_root` from init JSON and pass it in the Task prompt. The key agent-to-workflow mappings:

| Agent | Spawning Workflow(s) |
|-------|---------------------|
| gsd-planner | plan-phase.md, quick.md, verify-work.md |
| gsd-executor | execute-phase.md, quick.md |
| gsd-verifier | execute-phase.md, quick.md |
| gsd-debugger | diagnose-issues.md |
| gsd-project-researcher | new-project.md, new-milestone.md |
| gsd-research-synthesizer | new-project.md, new-milestone.md |
| gsd-roadmapper | new-project.md, new-milestone.md |
| gsd-codebase-mapper | map-codebase.md |
| gsd-phase-researcher | research-phase.md, discuss-phase.md |
| gsd-integration-checker | verify-phase.md |

For each workflow, check that the init JSON extraction block includes `planning_root` (or the variable the agent needs). If missing, add the extraction. Per CONTEXT.md: "Where interpolation wiring is missing, add it in the workflow."

Most workflows already extract `planning_root` from init (31 of 32 workflows have it per Phase 3). This step is a confirmation pass -- fix only if a gap is found.
  </action>
  <verify>
    <automated>echo "=== Agent file refs ===" && count=$(grep -rl '\.planning/' agents/ 2>/dev/null | wc -l | tr -d ' ') && echo "Files with .planning/ refs: $count" && if [ "$count" -ne "0" ]; then echo "FAIL - expected 0"; grep -rn '\.planning/' agents/; exit 1; fi && echo "=== Zero-ref agent confirmation ===" && for f in agents/gsd-nyquist-auditor.md agents/gsd-plan-checker.md; do c=$(grep -c '\.planning/' "$f" 2>/dev/null || echo 0); echo "$f: $c refs"; if [ "$c" -ne "0" ]; then echo "UNEXPECTED refs in $f"; exit 1; fi; done && echo "=== Workflow wiring ===" && for wf in get-shit-done/workflows/plan-phase.md get-shit-done/workflows/execute-phase.md get-shit-done/workflows/quick.md get-shit-done/workflows/new-project.md get-shit-done/workflows/new-milestone.md get-shit-done/workflows/map-codebase.md get-shit-done/workflows/research-phase.md get-shit-done/workflows/discuss-phase.md get-shit-done/workflows/verify-phase.md get-shit-done/workflows/diagnose-issues.md; do if grep -q 'planning_root' "$wf" 2>/dev/null; then echo "$wf: planning_root OK"; else echo "MISSING planning_root in $wf"; exit 1; fi; done && echo "PASS"</automated>
  </verify>
  <done>All 10 agent files have zero .planning/ literals. 2 zero-ref agents (gsd-nyquist-auditor.md, gsd-plan-checker.md) confirmed clean. All 12 agent files accounted for. All spawning workflows pass planning_root.</done>
</task>

</tasks>

<verification>
- `grep -rl '\.planning/' agents/ hooks/` returns zero results (excluding structural .planning/.active refs in hook which are acceptable)
- All 12 agent files accounted for: 10 converted + 2 confirmed zero-ref
- All spawning workflows extract and pass planning_root
- `npm test` passes (no regressions from hook change)
</verification>

<success_criteria>
- Context monitor uses .active-based namespace resolution
- All 10 agent files with refs use variable placeholders
- 2 zero-ref agents (gsd-nyquist-auditor.md, gsd-plan-checker.md) confirmed clean
- All 12 agents accounted for per roadmap
- Zero .planning/ hardcoded refs in agents/ directory
- Hook .planning/.active structural refs are the only .planning/ refs remaining in hooks/
- Every spawning workflow passes planning_root to its agent
</success_criteria>

<output>
After completion, create `.planning/phases/08-agent-command-path-variables/08-01-SUMMARY.md`
</output>
